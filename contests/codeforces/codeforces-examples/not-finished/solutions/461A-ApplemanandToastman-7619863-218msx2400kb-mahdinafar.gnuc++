#include<iostream>
#include<set>
#include <algorithm>
#include <map>
using namespace std;
long long n,m,b[310002],w,c,o,q,p,k,d,x,a,y;

int main(){
	cin>>n;
	for(int i=1;i<=n;i++){
	cin>>b[i];
	}
	sort(b+1,b+1+n);
	for(int i=1;i<n;i++){
		o+=b[i]*(i+1);
	}
	o+=b[n]*n;
	cout<<o;
}

